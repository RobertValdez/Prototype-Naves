using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PositionStart : MonoBehaviour {
	
	void Start () {

		/*
		object[] obj = GameObject.FindGameObjectsWithTag("Enemigo");
		GameObject g = null;
		foreach (object o in obj)
		{
			g = (GameObject) o;
			Debug.Log (g.transform.position);
			GameManager.sharedInstance.positionEnemy.Add(g.transform.position);
		}
		*/
	}
}
		